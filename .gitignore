/node_modules
/dist
.env
.env.*
!.env.example
*.log
.DS_Store
# TypeScript incremental build artifact — regenerated by every build, and committing it makes the
# working tree dirty after `npm run build` (which blocks `git pull` on the deploy box).
tsconfig.tsbuildinfo
